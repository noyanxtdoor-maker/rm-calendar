# Data boundary

Milestone 0 has no persisted data, credentials, or remote clients. Milestone 1 adds local Dexie data under `data/local`; Milestone 4 adds a typed Supabase adapter under `data/remote` and foreground sync under `data/sync`.
