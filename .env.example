# RM Calendar browser configuration (safe to commit; both values are public).
#
# Leave both blank for the local-only prototype. Set both only after a NEW,
# dedicated RM Calendar Supabase project has been approved and its RLS
# migrations have passed locally. Never put a service-role or SMTP secret here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
